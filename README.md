# rn_demo
这是一个react native的demo项目

# 关于时区的理解
1. 用户手机回默认本地的时区，比如北京的用户默认为东八区
2. 医生信息中携带当前的时区信息，当用户进行预约的时候，需要根据时区信息和当前手机时区，进行时间大小的比对，可以是时间戳或者UTC时间，设置一个可以预约的条件（比如当前时间30分钟以前的可以预约，过时的只能查看），
3. 在用户预约列表中，显示的时间是否到期，需要根据用户预约医生的时间信息，进行对比，可以是时间戳或者UTC时间，如果预约时间（UTC）小于当前时间，就可以取消，如果到了预约时间（UTC）大于当前时间，那么不可取消。