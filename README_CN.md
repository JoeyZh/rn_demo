
# MedScheduler - React Native Demo Project

## 📱 Overview

MedScheduler 是一个基于 React Native 构建的医疗预约管理应用。它允许用户查看医生信息、预约时间段，并管理自己的预约列表。本项目展示了如何在跨平台移动应用中处理时区差异、时间计算和状态持久化等核心功能。


## ⚙️ Setup & Usage Instructions

### Prerequisites

确保你已经安装以下工具：

- Node.js (v16 或更高版本,笔者是22.14.0)
- npm 或 yarn 或 pnpm
- React Native CLI 或 Expo CLI
- Android Studio / Xcode (用于模拟器)

### Installation

1. 克隆项目仓库：

   ```bash
   git clone https://github.com/your-repo/MedScheduler.git
   cd MedScheduler
   ```

2. 安装依赖项：

   ```bash
   npm install
   # 或者
   yarn install
   # 或者
   pnpm install
   ```

3. 启动开发服务器：

   ```bash
   # 如果使用 Expo
   npx expo start

   # 如果使用 React Native CLI
   npx react-native run-android
   # 或
   npx react-native run-ios
   ```

4. 在模拟器或真机上打开应用。

---

### Usage Guide

1. **首页**：展示医生列表及其可用时间段。
2. **预约页面**：选择医生和时间后提交预约请求。
3. **我的预约**：查看已预约的时间段，支持取消未过期的预约。
4. **时区处理**：自动根据医生时区和用户本地时区进行时间转换。

---

## 🧠 Assumptions & Design Decisions

### Key Assumptions

1. **时区处理**：
   - 用户设备默认使用本地时区（如北京时间 UTC+8）。
   - 医生信息中包含其所在时区（如 UTC-5）。
   - 所有时间计算均通过时间戳或 UTC 时间完成，确保跨时区一致性。

2. **时间比较逻辑**：
   - 预约时间必须晚于当前时间。
   - 已过期的预约无法取消。
   - 使用 `new Date().getTime()` 进行时间戳比较。

3. **状态管理**：
   - 使用 Redux Toolkit 管理全局状态（如医生列表、预约记录）。
   - 使用 AsyncStorage 持久化预约数据，存储医生列表，预约记录，防止应用重启丢失。

4. **导航设计**：
   - 使用 React Navigation 实现页面跳转。
   - 页面栈结构清晰，支持返回和重定向。

---

### Design Decisions

| 决策点 | 方案 | 原因 |
|--------|------|------|
| 状态管理 | Redux Toolkit | 支持复杂状态逻辑，易于调试和扩展 |
| 数据持久化 | AsyncStorage | 轻量级存储方案，适合移动端 |
| 时间处理 | UTC + 时间戳 | 避免时区混淆，保证准确性 |
| UI 组件库 | 自定义组件 | 更灵活地适配设计需求 |

---

## 🛠 Known Limitations & Future Enhancements

### Current Limitations

1. **网络请求模拟**：
   - 目前只有医生数据来自测试api。其他操作使用的的本地化的数据存储模拟后台存储
   - 建议后续集成 RESTful API 或 GraphQL。

2. **国际化支持缺失**：
   - 当前仅支持中文界面。
   - 可扩展为多语言支持（如英语、西班牙语）。

3. **性能优化不足**：
   - 大量预约数据可能导致渲染卡顿。
   - 建议引入虚拟滚动或分页加载机制。

4. **错误处理需完善**：
   - 异常场景（如网络中断、数据格式错误）因时间有限，demo版只包含基本的提示。
   - 需补充 Toast 或 Modal 形式的用户反馈。

### Future Enhancements

1. **离线模式支持**：
   - 利用 AsyncStorage 存储本地数据，存储医生列表，以及预约记录。支持离线操作。

---

## 🧪 Testing

### Test Coverage

本项目包含以下类型的自动化测试：

#### 1. Edge Cases
- 测试极端输入值（如无效时间戳、空数组）。
- 验证边界条件下的行为（如刚好到达预约时间点）。

#### 2. Negative Cases
- 模拟网络请求失败场景。
- 测试非法操作（如尝试取消已过期的预约）。

#### 3. Happy Path
- 正常流程的功能测试（如成功预约、正常取消）。

### Running Tests

```bash
# 运行所有测试
npm test

# 运行特定测试文件
npm test -- __tests__/appointment.test.ts
```

### Example Test Case

```ts
describe('Appointment Cancellation', () => {
  it('should prevent cancellation of expired appointments', () => {
    const expiredAppointment = {
      date: new Date(Date.now() - 86400000), // 昨天
      time: '10:00',
      isBooked: true,
    };

    expect(canCancelAppointment(expiredAppointment)).toBe(false);
  });
});
```

---

## 📦 Technical Stack

| 技术 | 版本 | 用途 |
|------|------|------|
| React Native | ≥ 0.70 | 核心框架 |
| Expo | 最新版 | 开发环境 |
| React Navigation | v6 | 页面导航 |
| Redux Toolkit | 最新版 | 状态管理 |
| AsyncStorage | 最新版 | 数据持久化 |
| TypeScript | 最新版 | 类型安全 |
| Jest | 最新版 | 单元测试 |

---

## ✅ License

MIT License. See [LICENSE](./LICENSE) for details.

---

如果你有任何问题或改进建议，请随时联系我！